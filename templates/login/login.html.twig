{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
    <div class="wrapper auth-wrapper" data-controller="svg-animation">
        {{ include('components/background/_pattern_patches.html.twig') }}

        <div class="auth-card">
            <form method="post">
                {% if error %}
                    <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}

                {% if app.user %}
                    <div class="mb-3">
                        You are logged in as {{ app.user.userIdentifier }}, <a href="{{ logout_path() }}">Logout</a>
                    </div>
                {% endif %}

                <header class="header-content">
                    <h1 class="title-alt">Please sign in</h1>
                </header>

                <label for="username" class="form-label">Email</label>
                <input type="email" value="{{ last_username }}" name="_username" id="username" class="form-control" autocomplete="email" required autofocus>
                <label for="password" class="form-label">Password</label>
                <input type="password" name="_password" id="password" class="form-control" autocomplete="current-password" required>
                <input type="hidden" name="_csrf_token" data-controller="csrf-protection" value="{{ csrf_token('authenticate') }}">

                {#
                    Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                    See https://symfony.com/doc/current/security/remember_me.html

                    <div class="checkbox mb-3">
                        <input type="checkbox" name="_remember_me" id="_remember_me">
                        <label for="_remember_me">Remember me</label>
                    </div>
                #}
                
                <div class="auth-actions">
                    {{ include('components/_button.html.twig', {'type': 'submit', 'label': 'Sign in'}) }}

                    <p class="mt-3">
                        <a href="{{ path('app_forgot_password_request') }}">Forgot password?</a>
                    </p>
                </div>
            </form>

            <p class="mt-3">
                Don't have an account? <a href="{{ path('app_register') }}">Register</a>
            </p>
        </div>
    </div>
{% endblock %}
